apiVersion: v1
kind: ServiceAccount
metadata:
  name: sam-router-sa
  namespace: ${NAMESPACE}
---
apiVersion: v1
kind: Service
metadata:
  name: sam-router-${ENV_NAME}
  namespace: ${NAMESPACE}
spec:
  clusterIP: None
  ports:
    - port: 4501
      targetPort: 4501
      protocol: TCP
      name: p2p-tcp
    - port: 4501
      targetPort: 4501
      protocol: UDP
      name: p2p-udp
  selector:
    app: sam-router-${ENV_NAME}
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: sam-router-${ENV_NAME}
  namespace: ${NAMESPACE}
spec:
  serviceName: "sam-router-${ENV_NAME}"
  replicas: 3
  selector:
    matchLabels:
      app: sam-router-${ENV_NAME}
  template:
    metadata:
      labels:
        app: sam-router-${ENV_NAME}
    spec:
      serviceAccountName: sam-router-sa
      securityContext:
        fsGroup: 65532
        fsGroupChangePolicy: "OnRootMismatch"
      containers:
      - name: sam-router
        image: ghcr.io/google/sam-router:${IMAGE_TAG}
        ports:
        - containerPort: 4501
          hostPort: 4501
          protocol: TCP
          name: p2p-tcp
        - containerPort: 4501
          hostPort: 4501
          protocol: UDP
          name: p2p-udp
        # Cluster-internal only: no hostPort, and nothing on it is authenticated.
        - containerPort: 9090
          protocol: TCP
          name: metrics
        args:
        - "--control-plane=http://sam-control-plane-${ENV_NAME}.${NAMESPACE}.svc.cluster.local:8080"
        - "--insecure-control-plane"
        - "--listen=/ip4/0.0.0.0/tcp/4501"
        - "--listen=/ip4/0.0.0.0/udp/4501/quic-v1"
        - "--external-addr=/dnsaddr/bootstrap.${ENV_NAME}.sam-mesh.dev"
        - "--jwt-path=/var/run/secrets/tokens/sam-token"
        - "--keys-path=/data/router.key"
        - "--metrics-addr=0.0.0.0:9090"
        # /healthz answers as soon as the process is up; /readyz once the
        # router has enrolled and its libp2p host is online.
        startupProbe:
          httpGet:
            path: /healthz
            port: metrics
          periodSeconds: 5
          failureThreshold: 24
        readinessProbe:
          httpGet:
            path: /readyz
            port: metrics
          periodSeconds: 10
        livenessProbe:
          httpGet:
            path: /healthz
            port: metrics
          periodSeconds: 20
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 500m
            memory: 512Mi
        volumeMounts:
        - name: sam-token
          mountPath: /var/run/secrets/tokens
          readOnly: true
        - name: router-data
          mountPath: /data
      volumes:
      - name: sam-token
        projected:
          sources:
          - serviceAccountToken:
              path: sam-token
              expirationSeconds: 3600
              audience: "sam-control-plane-audience"
  volumeClaimTemplates:
  - metadata:
      name: router-data
    spec:
      accessModes: [ "ReadWriteOnce" ]
      resources:
        requests:
          storage: 1Gi
